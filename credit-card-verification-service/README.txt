This microservice is responsible for below

// listen to NewCreditCardEvent -> Do the process -> publish the VerifyCreditCardEvent


queue in rabbitMQ can be seen only when we start the consumer.
else no queue is visible even though we implement the publisher code